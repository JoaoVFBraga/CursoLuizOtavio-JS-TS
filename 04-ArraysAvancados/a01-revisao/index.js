// Valor por referência

const nomes = ['Eduardo', 'Maria', 'Joana'];
// array.pop() = Remove um item do final;
// array.shift() = Remove um item do começo;
// array.push('x') = Adiciona um item no final;
// array.unshift('x') = Adiciona um item no começo;
// array.slice(1, 3) = Pega uma "fatia do array", começa no index 1 e vai até o 2;
// string.split(' ') = Transforma a string em um array separando os elementos pelo caracter escolhido;
