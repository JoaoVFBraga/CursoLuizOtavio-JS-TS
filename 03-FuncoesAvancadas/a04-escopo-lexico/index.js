const nome = 'João';

function falaNome() {
  consolew.log(nome);
};

falaNome();

// As funções conseguem acessar as váriaveis fora do seu escopo, porém, não é possível acessar de fora alguma váriavel dentro do escopo de uma função